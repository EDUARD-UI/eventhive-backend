package com.eventhive.app.enums;

// Permisos que el representante puede otorgar a un TRABAJADOR sobre eventos.
public enum PermisoEvento {
    CHECK_IN,
    CREAR_EVENTO,
    EDITAR_EVENTO,
    CANCELAR_EVENTO
}